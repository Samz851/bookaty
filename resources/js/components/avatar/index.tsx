import AvatarGenerator from 'react-avatar-generator';

export const RandomAvatar = () => {
    return (
        <AvatarGenerator
        colors={['#333', '#222', '#ccc']}
        backgroundColor="#000"
        />
    )
}
